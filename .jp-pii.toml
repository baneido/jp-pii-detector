# jp-pii-detecter 自身の設定（dogfooding）。
# 検出サンプルを含むファイルを走査から除外する。
min_confidence = "medium"

[allowlist]
paths = [
  "^testdata/",
  "^docs/",
  "^README\\.md$",
  "_test\\.go$",
  # 評価用データセット（ダミー PII を多数含む）
  "^internal/eval/dataset\\.go$",
]
