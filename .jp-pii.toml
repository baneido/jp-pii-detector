# jp-pii-detector 自身の設定（dogfooding）。
# 検出サンプルを含むファイルを走査から除外する。
min_confidence = "medium"

[allowlist]
paths = [
  "^testdata/",
  "^docs/",
  "^README\\.md$",
  "_test\\.go$",
  # 外部フィクスチャのローカルコピー（実在しうる PII を含む。.gitignore 済みで未コミット）
  "^pii-fixtures\\.json$",
  # Presidio 連携アダプタのサンプル/デモ（架空の PII を含む）
  "^integrations/",
]
